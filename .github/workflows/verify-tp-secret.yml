name: Verify TP Secret
on:
  workflow_dispatch:
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: presence secret
        env:
          S: ${{ secrets.TRADEPULSE_SECRET }}
        run: |
          if [ -n "$S" ]; then echo "SECRET_SET"; else echo "SECRET_EMPTY"; fi
